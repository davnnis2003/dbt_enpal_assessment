[project]
name = "dbt-enpal-assessment"
version = "0.1.0"
description = "dbt Enpal Assessment Project"
readme = "README.md"
requires-python = ">=3.11"
dependencies = [
    "dbt-core",
    "dbt-postgres",
]
