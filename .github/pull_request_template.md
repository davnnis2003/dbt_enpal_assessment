# Pull Request: Implementing CRM Analytics Pipeline (dbt + Postgres)

This PR is meant to:
1. 
2. 
3. 

## Checklist

- [ ] I have checked my models run and compile successfully locally (`dbt build` / `dbt compile`).
- [ ] My changes do not break downstream dependencies or reporting layer aggregations.
- [ ] All data tests (uniqueness, non-nullability, etc.) pass successfully.
- [ ] (Optional)I have updated the configuration files (`dbt_project.yml` and model schema YML files) for the models I worked on.
