version: 2

models:
  - name: mart__dim_crm_activity_types
    description: Tool-agnostic CRM dimension table for activity types.
    columns:
      - name: activity_type_id
        description: Primary key of the activity type.
        tests:
          - unique
          - not_null
